newsfeed_submenu:
  view:
    label: Info
    url:
      0: 'admin/post/view'
      id: $_GET*id
    select: action
  update:
    label: Update
    url:
      0: 'admin/post/update'
      id: $_GET*id
    select: action
  location:
    label: Storage Location
    url:
      0: 'admin/location'
      id: $_GET*id
    select: action
  views:
    label: View(s)
    url:
      0: 'view/admin/manage'
      archive: $_GET*archive
    select: controller
  delete:
    label: Delete
    url:
      0: 'admin/post/delete'
      id: $_GET*id
    htmlOptions:
      data-confirm: Are you sure you want to delete this item?
      data-method: post
    select: action